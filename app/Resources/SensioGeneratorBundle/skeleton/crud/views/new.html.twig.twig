{% block extends %}
{{ "{% extends 'base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block content %}" }}
    <div class="panel panel-flat">
        <div class="panel-heading">
            <h6 class="panel-title">{{ entity_singularized|capitalize }} creation<a class="heading-elements-toggle"><i class="icon-more"></i></a></h6>
            <div class="heading-elements">
                <a href="{{ "{{ path('" ~ route_name_prefix ~ "_index') }}" }}" class="btn btn-primary btn-xs "><b><i class="icon-loop3"></i></b> Retour à la liste</a>
            </div>
        </div>
        <div class="panel-body">
            <p class="content-group-lg"></p>

            {{ '{{ form_start(form, {\'attr\': {\'class\': \'form-horizontal\'}}) }}' }}
            {{ '{{ form_widget(form) }}' }}
            <div class="text-right">
                <button type="submit" class="btn btn-primary legitRipple">Envoyer <i class="icon-arrow-right14 position-right"></i></button>
            </div>
            {{ '{{ form_end(form) }}' }}
        </div>
    </div>

{% set hide_edit, hide_delete = true, true %}
{# {{ include('crud/views/others/record_actions.html.twig.twig') -}} #}
{{ "{% endblock %}" }}
{% endblock body %}
