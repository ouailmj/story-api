{% trans_default_domain 'FOSUserBundle' %}
{% form_theme form 'Form/fields.html.twig' %}

{% block content %}
<div class="panel panel-flat">
    <div class="panel-heading">
        <h6 class="panel-title">Mettre à jour le mot de passe <a class="heading-elements-toggle"><i class="icon-more"></i></a></h6>
    </div>

    <div class="panel-body">
        <p class="content-group-lg"></p>
{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password form-horizontal' } }) }}
    {{ form_widget(form) }}
    <div class="form-group pull-right">
        <button name="update" type="submit" class="btn btn-primary legitRipple">{{ 'change_password.submit'|trans }} <i class="icon-arrow-right14 position-right"></i></button>
    </div>
{{ form_end(form) }}
    </div>
</div>

{% endblock  %}